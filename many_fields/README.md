# many\_fields

Load this app using

```python
load_config(uproot_server, config="many_fields", apps=["many_fields"])
```

Internal benchmark that tests inserting a large number of fields (50 × 50) on player objects. Not intended as a template for building apps.
