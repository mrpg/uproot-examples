# chat\_with\_gpt

Load this app using

```python
load_config(uproot_server, config="chat_with_gpt", apps=["chat_with_gpt"])
```

*Note*: This app requires the dependency `openai` and you need to set the environment variable `OPENAI_API_KEY`.
