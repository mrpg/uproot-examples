# prisoners\_dilemma\_chat

Prisoner's dilemma with a continuous chat between the two players. The chat persists across all pages after grouping, including the synchronizing wait page.

Load this app using

```python
load_config(uproot_server, config="prisoners_dilemma_chat", apps=["prisoners_dilemma_chat"])
```
