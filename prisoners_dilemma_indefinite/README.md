# Indefinitely repeated prisoner's dilemma

Add `prisoners_dilemma_indefinite` to `main.py`:

    load_config(uproot_server, config="prisoners_dilemma_indefinite", apps=["prisoners_dilemma_indefinite"])
