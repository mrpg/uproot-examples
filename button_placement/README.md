# button_placement

Demonstrates `button_next()` and `button_back()` for placing navigation buttons at arbitrary positions with custom styling. When either is called, the default buttons are suppressed.

Load this app using

```python
load_config(uproot_server, config="button_placement", apps=["button_placement"])
```
