# between

Load this app using

```python
load_config(uproot_server, config="between", apps=["between"])
```

This app demonstrates `Between(...)`, which randomly selects exactly one option
from the pages it contains. Use `Bracket(...)` when one option should contain
multiple consecutive pages.
