# emoji_sort

Emoji sorting task for kids. Participants drag each emoji into either the NICE or NASTY bucket. Each placement is tracked individually via a hidden RadioField (`1` = NICE, `2` = NASTY).

## Load this app

```python
load_config(uproot_server, config="emoji_sort", apps=["emoji_sort"])
```
