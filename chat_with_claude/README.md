# chat\_with\_claude

Load this app using

```python
load_config(uproot_server, config="chat_with_claude", apps=["chat_with_claude"])
```

*Note*: This app requires the dependency `anthropic` and you need to set the environment variable `ANTHROPIC_API_KEY`.
